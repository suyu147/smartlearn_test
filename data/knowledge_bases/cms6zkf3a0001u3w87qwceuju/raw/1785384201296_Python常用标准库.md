# Python 常用标准库

## 系统与路径

### os — 操作系统接口
```python
import os
os.getcwd()           # 当前工作目录
os.listdir(".")       # 列出目录
os.path.join("a","b") # 路径拼接
os.path.exists(path)  # 判断存在
```

### sys — 解释器交互
```python
import sys
sys.argv          # 命令行参数
sys.exit(0)       # 退出
sys.path          # 模块搜索路径
```

## 文件与数据

### json
```python
import json
json.dumps(data, ensure_ascii=False)
json.loads(json_str)
```

### csv
```python
import csv
with open("data.csv") as f:
    reader = csv.DictReader(f)
    for row in reader: print(row)
```

### re — 正则表达式
```python
import re
re.search(r"\d+", text)
re.findall(r"\w+", text)
re.sub(r"\s", "_", text)
```

## 数据处理

### itertools
```python
from itertools import chain, product, permutations, groupby
```

### collections
```python
from collections import defaultdict, Counter, deque, namedtuple
```

## 网络

### urllib
```python
from urllib.request import urlopen
from urllib.parse import urlparse, quote
```

## 并发

```python
import threading, multiprocessing, asyncio, concurrent.futures
```
